$ cd /Users/denis.lantsman/src/edulab && sed -n 148,172p plans/2026-09-05-nested-learning-threads.md
stdout:
The live selection is not a property of a thread: only the left pane captures one, so a single `anchor: Anchor | null` lives in the prototype's own state alongside `focus` and `split`.
`packages/frontend/prompt.ts` (pure, unit tested):
```ts
export const LEARNING_SYSTEM: string;
/** The single seed user turn for a learning thread. */
`seedTurn` takes one `Step` per level of `path(anchor.thread)`, oldest first; the last is the selection being acted on now. Each step's `messages` are that thread's visible messages, already truncated after the message its anchor ends in.
```ts
export type Step = {
  messages: ReadonlyArray<Message>;
  anchor: Anchor;
  action: Action;
};
export const LEARNING_SYSTEM: string;
/** The single seed user turn for a learning thread. */
export function seedTurn(steps: ReadonlyArray<Step>): string;
```
`packages/frontend/conversation.ts`:
```ts
constructor(socket: Socket, opts?: {
exit code 0
